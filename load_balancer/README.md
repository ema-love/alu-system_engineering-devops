# Load Balancer

This project covers configuring Nginx web servers and an HAProxy load balancer.

## Tasks
- Configure Nginx to return the `X-Served-By` HTTP response header.
- Configure HAProxy to distribute requests between two web servers using the round-robin algorithm.
